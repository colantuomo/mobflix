import { Plus } from '../../Icons';
import { ButtonContainer, RoundedButton } from './styles';

export function FloatButton() {
  return (
    <ButtonContainer>
      <RoundedButton>
        <Plus />
      </RoundedButton>
    </ButtonContainer>
  );
}
